name: pull

# PR CI: lint plus one CPU smoke build. The full platform/variant wheel
# matrix runs on trunk (main pushes) and release tags, not on every PR.

on:
  pull_request:
    branches-ignore:
      - nightly
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

permissions: read-all

jobs:
  smoke-build:
    name: pull-smoke-build
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/_binary-build-test.yml
    with:
      build_name: tensorplay-linux-x86_64-py3_12-cpu
      build_environment: pull-smoke-linux-x86_64-py3_12
      runs_on: ubuntu-22.04
      python_version: "3.12"
      test_matrix: '[{"name":"native-sparse","test_files":"test/test_set_native.py test/test_compressed_sparse_constructors.py"},{"name":"nn-utils","test_files":"test/test_nn_utils_norm.py"}]'
    secrets: inherit
